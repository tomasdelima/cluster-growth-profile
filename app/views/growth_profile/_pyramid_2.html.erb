<div id="pyramid-2" class="list colspan-21">
  <div class="col-21 no-border left">
    <h3>Crescimento no ciclo (de <%= GrowthProfile.to_gregorian_cycle(params[:cycle], :long)[:beginning_of_cycle] %> até <%= GrowthProfile.to_gregorian_cycle(params[:cycle], :long)[:end_of_cycle] %>)</h3>
    <button class="help" ng-click="showHelp('Esta pirâmide é destinada a medir apenas o crescimento do ciclo.')">?</button>
  </div>

  <%= (1..8).each do |l| %>
    <div class="col-21 list">
      <div class="col-<%= 8 - l %> no-border"></div>
      <div class="col-1 no-border">L<%= 9 - l %></div>
      <div class="col-<%= l + 2 %>"><input type="number" ng-change="changeFormState()" ng-model="growth_profiles[3].growth_pyramid.finishedBook<%= l %>"></div>
      <div class="col-<%= l + 2 %>"><input type="number" ng-change="changeFormState()" ng-model="growth_profiles[3].growth_pyramid.studyingBook<%= l %>"></div>
      <div class="col-<%= 8 - l %> no-border"></div>
    </div>
  <% end %>

  <div class="col-21 list">
    <div class="col-1 no-border"></div>
    <div class="col-10 no-border with-tooltip">
      Concluído
      <md-tooltip class="md-subhead" layout-padding md-direction="bottom">Indica apenas o número de pessoas que concluíram o estudo de cada um dos livros durante o ciclo que se encerra, independente de quando o iniciaram.</md-tooltip>
    </div>
    <div class="col-10 no-border with-tooltip">
      Estudando
      <md-tooltip class="md-subhead" layout-padding md-direction="bottom">Indica somente o número de novas pessoas que iniciaram seus estudos dentro do ciclo.</md-tooltip>
    </div>
  </div>
</div>
