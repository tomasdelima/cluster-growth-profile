<div id="table-11" class="list colspan-7">
  <div class="col-7 no-border left">
    <h3>
      Avanço na sequência dos cursos do instituto
      <button class="help" ng-click="showHelp('Esta tabela é destinada a verificar o número de indivíduos que avançam na sequência dos cursos do Instituto Ruhí neste ciclo.')">?</button>
    </h3>
  </div>

  <div class="col-7 list header">
    <div class="col-1">Ciclos</div>
    <div class="col-3 with-tooltip">
      Crianças → Grupos de Pré-Jovens
      <md-tooltip class="md-subhead" layout-padding md-direction="top">Escrever apenas o número de crianças que estavam estudando em Aulas para Crianças que iniciaram o primeiro livro do programa de pré-jovens.</md-tooltip>
    </div>
    <div class="col-3 with-tooltip">
      Pré-Jovens → Círculos de Estudo
      <md-tooltip class="md-subhead" layout-padding md-direction="top">Escrever apenas o número de pré-jovens que estavam participando do programa de pré-jovens e que iniciaram o Livro 1 da sequência de círculos de estudo.</md-tooltip>
    </div>
  </div>

  <% (0..3).each do |c| %>
    <div class="col-7 list hover">
      <div class="col-1 header">{{growth_profiles[<%= c %>].gregorian_cycle.end_of_cycle}}</div>
      <% ['converted_junior_youth_groups', 'converted_study_circles'].each do |f| %>
        <div class="col-3">
          <% if c < 3 %>
            &nbsp;{{growth_profiles[<%= c %>].<%= f %>}}&nbsp;
          <% else %>
            <input ng-change="changeFormState()" type="number" ng-model="growth_profiles[3].<%= f %>">
          <% end %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
