<div id="table-11" class="list colspan-7">
  <div class="col-7 no-border">
    <h3>Avanço na sequência dos cursos do instituto</h3>
  </div>

  <div class="col-7 list header">
    <div class="col-1">Ciclo</div>
    <div class="col-3">Crianças → Grupos de Pré-Jovens</div>
    <div class="col-3">Pré-Jovens → Círculos de Estudo</div>
  </div>

  <% (0..3).each do |c| %>
    <div class="col-7 list hover">
      <div class="col-1 header">{{growth_profiles[<%= c %>].cycle}}</div>
      <% ['converted_junior_youth_groups', 'converted_study_circles'].each do |f| %>
        <div class="col-3">
          <% if c < 3 %>
            &nbsp;{{growth_profiles[<%= c %>].<%= f %>}}&nbsp;
          <% else %>
            <input ng-change="changeFormState()" type="number" ng-model="growth_profiles[3].<%= f %>">
          <% end %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
